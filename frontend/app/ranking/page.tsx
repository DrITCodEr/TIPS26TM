import { StubPage } from "@/components/layout/StubPage";

export default function RankingPage() {
  return (
    <StubPage title="Ranking" emoji="🏆" phase={3}>
      Top-4-Favoriten als Stat-Cards plus sortierte Liste aller 48 Teams mit
      P(Titel), P(Finale), P(HF), P(VF).
    </StubPage>
  );
}
